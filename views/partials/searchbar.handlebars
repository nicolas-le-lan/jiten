<form method="get" action="/search/" class="d-flex">
  <input class="form-control form-control-lg ms-4 me-2 bg-light text-black"  type="search" required="" maxlength="50" name="word" value="{{data}}" placeholder="English, Japanese, Romaji, word or text">
  <button class="btn btn-outline-success" type="submit">Search</button>
</form>